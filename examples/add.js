'use strict';
/**
 * Adds two numbers
 *
 * @param {Number} x
 * @param {Number} y
 *
 * @example
 *    add(10, 20)
 *    // => 30
 *    add(10, 50) // => 60
 */

function add(x, y) {
  return x + y;
}
exports.add = add;
